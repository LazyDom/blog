name: Lint, URL Check, Build & Deploy Jekyll Blog

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  lint-yaml:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install yamllint
        run: pip install yamllint

      - name: Lint YAML files (including Jekyll front matter)
        run: |
          yamllint . --format colored --strict

  build-and-deploy:
    runs-on: ubuntu-latest
    needs: lint-yaml
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'

      - name: Install dependencies
        run: |
          gem install bundler
          bundle install

      - name: Build Jekyll site
        run: bundle exec jekyll build

      - name: Check GitHub links in built site (lychee-action)
        uses: lycheeverse/lychee-action@v2.4.1
        with:
          args: >-
            --verbose
            --no-progress
            --include 'github.com'
            --exclude '^/blog/'
            --exclude '^/assets/'
            --exclude '^/images/'
            './_site/**/*.html'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check non-GitHub links in built site (lychee-action)
        uses: lycheeverse/lychee-action@v2.4.1
        with:
          args: >-
            --verbose
            --no-progress
            --exclude 'github.com' --max-concurrency 2
            --exclude '^/blog/'
            --exclude '^/assets/'
            --exclude '^/images/'
            './_site/**/*.html'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
